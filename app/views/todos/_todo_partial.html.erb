<%= form_for [trip, todo], :html=> {class: 'todo_form'} do |f| %>
  <% if todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo.errors.count, "error") %> prohibited this To-Do from being saved:</h2>

      <ul>
      <% todo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field new-todo">
    <%= f.label "Add an item or task to the list!" %>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <input id='todo-button' type="submit" name="commit" value="Submit" data-disable-with="To Do">
  </div>
<p id="trip-id"><%= @trip.id %></p>
<% end %>
